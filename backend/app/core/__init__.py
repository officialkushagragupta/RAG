"""
Core infrastructure.

Application configuration (config.py), prompt templates (prompts.py),
external client wiring for ChromaDB and Gemini (chroma_client.py,
gemini_client.py), and custom exceptions (exceptions.py). Code here is
infrastructure, not feature logic, and is safe to depend on from anywhere
in the app.
"""
