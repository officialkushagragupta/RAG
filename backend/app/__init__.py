"""
Backend application package.

Contains the FastAPI app (main.py) and all of its sub-packages:
api/, services/, models/, core/, middleware/, and utils/.
See each sub-package's __init__.py for what it holds.
"""
