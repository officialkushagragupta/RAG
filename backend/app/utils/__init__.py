"""
Generic utilities.

Logging setup (logger.py), fixed non-configurable constants (constants.py),
and standalone input validators (validators.py). Anything here should be
free of framework/business-logic dependencies where possible.
"""
